# Create a function that takes a boolean variable flage and returns it as a string.
def bool_to_string(flag):
    string = str(flag)
    return string

st = bool_to_string(True)
print(st)
print(st + st)
