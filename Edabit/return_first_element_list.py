# Create a function that takes a list containing only numbers and return the
# first element.
def get_first_value(number_list):
    number = (number_list[0])
    return number

a = get_first_value([-500, 5, 100])
print(a)
