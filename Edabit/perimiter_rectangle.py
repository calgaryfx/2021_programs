# Create a function that takes length and width, calculate the perimiter of a
# rectangle.
def find_perimiter(length, width):
    perimiter = (length + width) * 2
    return perimiter

a = find_perimiter(20, 10)
print(a)
