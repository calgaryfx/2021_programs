# Create a function that takes a base number and an exponent number and returns
# the calculation.
def calculate_exponent(num, exp):
    number = num ** exp
    return number

n = calculate_exponent(3, 3)
print(n)
